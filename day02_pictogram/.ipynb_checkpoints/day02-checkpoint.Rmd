```{r}
library(tidyverse)
library(echarts4r)

setwd(here::here())
```

```{r}
getwd()
```

```{r}
starwars_char <- c(
  "Yoda", "Darth Vader", "Luke Skywalker", "C-3PO", "R2-D2", 
  "Leia Organa", "Obi-Wan Kenobi", "Chewbacca", "Han Solo"
)

pic_path <-starwars_char %>%
  map(~glue::glue("data/starwars/{.x}.png"))
```

```{r}
starwars %>%
  filter(!is.na(height), name %in% starwars_chars) %>%
  arrange(height) %>%
  pull(name)
```

```{r}
starwars %>%
  filter(name %in% starwars_char[1:2]) %>%
  select(name, height) %>%
  mutate(
    symbol = glue::glue("image://https://raw.githubusercontent.com/piyayut-ch/30DayChartChallenge/main/data/starwars/{name}.png")
  ) #%>% 
#   e_charts(name) %>% 
#   e_pictorial(height, symbol) %>% 
#   e_legend(FALSE) %>% 
#   e_title("Images", "Height")
```

```{r}
qomo <- paste0(
  "https://echarts.apache.org/examples/",
  "data/asset/img/hill-Qomolangma.png"
)

kili <- paste0(
  "https://echarts.apache.org/examples/", 
  "data/asset/img/hill-Kilimanjaro.png"
)

data <- data.frame(
  x = c("Qomolangma", "Kilimanjaro"), 
  value = c(8844, 5895),
  symbol = c(paste0("image://", qomo),
    paste0("image://", kili))
)

data %>% 
  e_charts(x) %>% 
  e_pictorial(value, symbol) %>% 
  e_legend(FALSE) %>% 
  e_title("Images", "Mountains height")
```

```{r}
qomo <- paste0(
  "https://echarts.apache.org/examples/",
  "data/asset/img/hill-Qomolangma.png"
)

kili <- paste0(
  "https://echarts.apache.org/examples/", 
  "data/asset/img/hill-Kilimanjaro.png"
)

data <- data.frame(
  x = c("Qomolangma", "Kilimanjaro"), 
  value = c(8844, 5895),
  symbol = c(paste0("image://", qomo),
    paste0("image://", kili))
)

data %>% 
  e_charts(x) %>% 
  e_pictorial(value, symbol) %>% 
  e_legend(FALSE) %>% 
  e_title("Images", "Mountains height")
```
